<%= form_tag({:action => :index, :controller => 'models'}, :method => :get ) do |f| %>

    <p>
      <label>Escolha a Fabricante:</label>
      <%= select_tag(:webmotors_make_id, options_from_collection_for_select(@makes, :webmotors_id, :name) ) %>
    </p>

    <%= submit_tag("Buscar modelos")%>
<% end %>
